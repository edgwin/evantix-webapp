import { DisableDownloadDirective } from './disable-download.directive';

describe('DisableDownloadDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDownloadDirective();
    expect(directive).toBeTruthy();
  });
});
