import { AiEditableDirective } from './ai-editable.directive';

describe('AiEditableDirective', () => {
  it('should create an instance', () => {
    // const directive = new AiEditableDirective();
    // expect(directive).toBeTruthy();
  });
});
